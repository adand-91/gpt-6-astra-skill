# HANDOFF

## 我们在做什么

Publish `v0.2.0-beta.1` as a non-latest GitHub prerelease, then report current repository metrics.
The authorised scope is the existing daily/weekly scaffold increment, not later Jarvis features.

## 完成了什么

- Reconstructed from published Alpha 3 in an isolated release worktree.
- Verified frozen Beta 1 wheel, sdist, and source SHA-256 values; copied only version metadata,
  review/CLI changes and their two test files. Preserved prior public history.
- Updated bilingual release documentation and added installed daily/weekly CI smoke coverage.
- Source and extracted-sdist suites passed 138 tests each; translation, compile and diff checks passed.
- Clean wheel/sdist installs report 0.2.0b1 and pass audit/daily/weekly create/check, restrictive
  permissions, no-overwrite, malformed input, and deterministic Demo checks.
- Independent bounded code review found no release blocker; its weekly CLI coverage suggestion
  is covered by both local installed-package smoke and the added CI step.

## 卡在哪儿

No known local release blocker. Public exact-commit CI and publication/download gates remain pending.

## 下一步计划

Push the candidate, pass Linux/macOS/Windows CI, publish the immutable annotated tag and prerelease,
then download and verify assets before reporting metrics. Stop after the Beta 1 receipt.

## 踩过哪些坑

Frozen source contained AppleDouble metadata and obsolete Alpha publication status. Neither was
copied. Current cumulative 1.0.0 source is not a Beta 1 release snapshot. Translation hash stamps
must follow actual translated changes; the initial stale-stamp failure was corrected and rerun.

## 当前任务汇总

- Package: 0.2.0b1. Intended tag: v0.2.0-beta.1. Public baseline: Alpha 3.
- Local qualification passed; public publication is pending.
- User authorised Beta 1 commit/push/CI/tag/Release/assets and repository metrics inspection.
- Later versions, repository rename, promotion, plugin submission and programme application remain excluded.

## 当前架构与入口

`src/requirement_ledger/review.py` calculates windows and builds empty private scaffolds;
`cli.py` exposes explicit arguments. No history discovery, network or scheduler was added.

## 运行与依赖

Python 3.10–3.13; Windows uses the existing conditional tzdata dependency.
Tests: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest discover -s tests -q`.
Build: `python -m build --sdist --wheel` in isolated build tooling.

## 验证证据

Source/extracted-sdist: 138 tests each. Translation: `python3 scripts/check_translation_sync.py`.
Compile: `python3 -m compileall -q src scripts tests`. Whitespace: `git diff --check`.
Archive inspection rejects generated bytecode, private JSON, maintainer paths and later modules.
Public hashes and CI receipt will be recorded after those actions actually complete.

## 回滚

Use reviewed forward commits before publishing. Never move published tags or replace assets.
The cumulative development worktree and previous releases remain preserved.
