<!-- translation-of: SKILL.md sha256:b379a6c2bf16552f -->
<!-- SKILL.md 是权威源。改动先改英文，再用 scripts/check_translation_sync.py --update 重新盖章。 -->

# 需求账本

Agent 最贵的失误不是 bug，是**把没人要的东西做完了**，或者**在悄悄缩小的范围上报「完成」**。
这个 Skill 只维护一本账：开工时把它立起来，收尾时把它结清。

非规范性的背景和真实失败案例见 [anti-patterns.zh-CN.md](references/anti-patterns.zh-CN.md)。

## 三层标注

每条需求只能处于一个状态，而且标签对用户始终可见：

| 标签 | 含义 | 谁能改 |
|---|---|---|
| `已确认` | 用户说过，或用户认可了你的复述 | 只有用户 |
| `AI推断` | 你选的默认值，因为猜错的代价可回滚 | 你可以定，但标签必须留着 |
| `待确认` | 答案会改变交付物、成本、工期或验收 | 要么问，要么由用户明确搁置 |

一条硬规则：**`AI推断` 永远不能被写成 `已确认`** —— 不在总结里，不在方案里，不在提交信息里，
不在状态行里。这份文档其余内容都是流程，只有这一条是诚信问题。

## 开工

1. **先复述再提问。** 一小段话：你认为目标是什么，你认为做完之后长什么样。复述不等于确认 ——
   把它作为候选理解交出去。
2. **从请求、文件和仓库里立第一版账本。** 对话里或磁盘上已经能回答的，一开始就是 `已确认`，
   不再问第二遍。
3. **一轮最多问三个问题**，按答案能改变多少结果排序。对方答不上你的行话时，别把术语原样丢回去：
   给两三个具体选项，说清各自的代价，并点明你的推荐。细则见
   [clarify-loop.zh-CN.md](references/clarify-loop.zh-CN.md)。
4. **闭环**：小批 → 回答 → 更新账本 → 下一批。绝不一次倾倒长问卷。

还有 `待确认` 项时，只读调查、诊断，以及用户授权过的可回滚探路都可以继续做；
不可逆或高成本的动作不行。

## 什么时候不该问

提问不是免费的，把人问烦本身就是一种失败。对每个 `待确认` 项只做一个测试：

> 如果我猜错了，撤销要付多少代价？

- **撤销便宜** —— 自己定，标 `AI推断`，用一行说明白，继续做。
- **撤销昂贵** —— 对外、破坏性、难回滚，或者会改变价格、工期和验收口径的 —— 做之前先问。

自己能查的不要问。同一件事不要问第二遍。用户说「你定」时，剩下所有 `待确认` 转为 `AI推断`，
直接推进。

## 锁定

只要超出「一步可回滚」的量级，动手前就出确认单。字段固定 —— 目标、包含、不包含、输入、输出、
验收、约束、执行位置、未确认项 —— 一屏之内，不写散文。模板和字段规则见
[confirmation-sheet.zh-CN.md](references/confirmation-sheet.zh-CN.md)。

只有用户明确说「无误」，或明确说「这些你定」，确认单才算锁定。沉默不算锁定。
依赖某份确认单之前先跑机械预检：

```bash
python3 scripts/check_confirmation_sheet.py path/to/sheet.md --lang zh
```

`VALID_SHEET` 表示必备字段齐、没有一条同时挂两个标签、没有 `AI推断` 被偷偷提级。
它只查形式不查真伪，也不能替代用户那句确认。

## 锁定之后

每个新来的要求，在动手之前先当面归类：

- **`缺陷`** —— 交付物没做到确认单写明的事。直接修，不重新谈，不算新工作量。
- **`原范围优化`** —— 在已锁范围内，没有新交付物。做掉并记账。
- **`新增范围`** —— 新交付物、新接触面，或验收口径变了。回到账本再说：
  在用户确认前它是 `待确认`，绝不悄悄进来。

规则和它防的那种失败见 [change-control.zh-CN.md](references/change-control.zh-CN.md)。

## 收尾

回溯到本次任务**最早**那条需求，不是最近几轮，把每条账目结成且仅结成一种状态：

`已完成` · `部分完成` · `明确取消` · `受阻`

任何一条都不许静默漏掉。`部分完成` 和 `受阻` 必须写清缺什么、为什么。
启动了进程、派出了 agent、写了方案，都不是 `已完成`。完整流程见
[closeout.zh-CN.md](references/closeout.zh-CN.md)。

## 账本放在哪

任务短的时候就放在回复里。一旦跨对话，就写到工作旁边的磁盘上 —— 账本正是下一个 agent、
或者上下文重置后的你自己，最先要读的东西。项目已经有接手或检查点文件时，账本进那个文件，
不要另起一个互相打架的新文件。
