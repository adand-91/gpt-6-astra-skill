<!-- translation-of: ROADMAP.md sha256:beabab619d983c52 -->

# Astra Skill Optimizer 路线图

Astra Skill Optimizer 是当前公开产品和仓库身份。历史 Requirement Ledger Python 核心与插件入口在迁移期间
作为兼容组件保留；本路线图追踪 Astra Skill Optimizer 适配和长期的 Jarvis 式系统，不代表新版本已经发布。

[English](ROADMAP.md) · [产品完工合同](docs/V1_PRODUCT_CONTRACT.zh-CN.md) ·
[更新地图](UPDATE_MAP.zh-CN.md) ·
[稳定契约](docs/V1_STABLE_CONTRACT.zh-CN.md)

## 两条完工线

包版本和公开产品体验有关联，但不能互相替代。

### 本地技术核心

```text
明确目标／窗口 + 明确范围／文件
  -> 私有审查骨架和已检查报告
  -> 无路径来源身份 + 候选延续
  -> 最终报告精确绑定
  -> 只读交接复验
  -> 如有需要，再单独授权实施
```

历史 CLI／库继续作为兼容入口保留。当前产品层是针对一个选定项目及其明确关联 Skill 的适配工作流；不新增私有凭据、
外部服务依赖或执行权限。

`audit`、`daily` 和 `weekly` 已实现，但只处理用户或宿主明确提供的目标／窗口／范围／文件。任何模式都不会
发现全部本地历史。

### Astra Skill Optimizer 公开产品

公开产品名是 Astra Skill Optimizer。短期目标是把现有项目 Skill 与流程适配到 GPT-6／Astra；长期发展为类似 Jarvis 的
个人与社区 Skill 优化系统。v1 用户流程是：

```text
一句自然语言接管
  -> 有界恢复目标／检查点
  -> 直接、解释或完整汇报的回答深度
  -> 六类生命周期场景之一，或独立日报／周报
  -> 单独授权后的有界持续推进
  -> 高影响结论需要精确证明时升级证据
  -> 可靠检查点与交接
```

准确的产品门以[`docs/V1_PRODUCT_CONTRACT.zh-CN.md`](docs/V1_PRODUCT_CONTRACT.zh-CN.md) 为准。
本地 Python `1.0.0` 候选不能单独证明公开产品门已经通过。

## 稳定承诺

v1.0 保证保守、离线的身份处理：检查明确文件，把最终报告绑定到所选来源／候选状态，并在交接前复验该身份。
它不承诺语义真实、完整历史覆盖、自主诊断或任何执行权限。

尤其是，`review-handoff-check` 成功不授权代码修改、commit、push、Issue、Release、上传、消息或公开插件提交。
不完整证据会阻断正常交接；显式的不完整归档模式只用于保留。

v0.1 显式证据 CLI 工作流继续可用，可兼容地采集证据、分析、生成报告／建议并进行冻结 oracle 对比。

## 发布状态

本地候选只有在本地测试、构建、干净安装、命令工作流、插件检查和文档一致时才算完成。在维护者单独授权并
完成外部发布流程之前，它始终是本地候选。

后续流程包括：决定准确 commit、commit／tag／push、发布资产、复验公开资产，并遵循当时 Codex 插件提交要求。
产品不会自动执行这些步骤，本路线图也不表示已执行。

## 产品推进顺序

1. 快速第一次接管与说人话汇报——本地已完成。
2. 三种回答深度、完整八项项目汇报和六场景路由——本地已实现，等最后一轮全新任务 dogfood。
3. 独立日报／周报模板、严格证据升级、有界资源发现和反馈驱动改进路由——本地已实现，
   同样等待最后验收。
4. Windows／公开 CI、公开打包、发布时改名和真实外部采用证据。

第 4 步外部动作仍按产品合同单独授权，不能从前一项本地完成自动推导。

## 产品 v1 之后

### `1.0.x`

只发布有证据支持的 Bug、安全／隐私、兼容性、打包或文档修复。没有变化就不发布。

### `1.1`

只有先规定并测试同意模型、准确输入边界、私有数据保留、失败处理和移除路径后，才考虑新增 opt-in 集成。
新集成不能削弱明确来源选择，也不能引入隐藏权限。

### 更后续版本

其他 provider 适配与用户体验改进可以从真实使用证据中规划。在单独授权的设计建立安全与运营边界前，
它们不在范围内。

语音唤醒、桌面宠物、无人值守夜间调研和广泛跨论坛采集属于 v1 之后的想法。实施前需要单独定义运行时、隐私、失败和资源边界。
