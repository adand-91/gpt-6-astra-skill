<!-- translation-of: ROADMAP.md sha256:b1defdae8a51952c -->

# 路线图

Requirement Ledger AI 正从已发布的证据原型，用十天、逐门禁的冲刺走向聚焦、可用的
`v0.2.0`。日期只是目标，不能作为发布未完成内容或制造活动的理由。每个公开版本都必须代表一组
连贯、经过测试的真实能力。

[English](ROADMAP.md) · [十天更新地图](UPDATE_MAP.zh-CN.md) ·
[未完成项](docs/PROJECT_GAPS.md)

## 产品终点

稳定 `v0.2.0` 是一个 AI 项目与 Agent Skill 反馈闭环：

```text
点名的 Codex 目标或审查窗口
  -> 相关且已授权的历史 + 显式 Git／测试证据
  -> 私有、可追溯发现
  -> 一次性、日报或周报改进报告
  -> 经审查、由宿主控制的 Codex 修改
  -> 同一 oracle 的修改前后证据
```

Codex 宿主负责有界的相关上下文发现；CLI 始终是显式文件证据与校验层。两者都不会静默扫描无关
历史、运行项目代码、编辑工作树或执行 GitHub／账号动作。

“十天完善”指稳定、可用的 v0.2 契约、安装包、三模式工作流、文档、示例、CI 与发布证据；不表示
所有未来集成、provider 或个人工作流都已经完成。

## 十天 v0.2 完善路线

公开目标序列为 `alpha.1 → alpha.2 → alpha.3 → beta.1 → beta.2 → beta.3 → rc.1 →
rc.2 → rc.3 → v0.2.0`，每个发布日只推出一个有真实增量的候选版。下列章节描述较大的能力
门禁，精确逐日拆分以[更新地图](UPDATE_MAP.zh-CN.md)为准；任一门禁失败就顺延列车，不发布空版本。

### `v0.2.0-alpha.1`——可安装的点名目标审查

- 面向一个显式目标和窗口的私有、不覆盖 `review-init --mode audit` 骨架。
- 零依赖 `review-check`，校验结构化报告、证据、时区与授权门禁。
- 双语宿主契约、参考模板、更新说明和更新地图。
- 已发布 v0.1.1 证据 CLI 保持兼容。

退出门：两种资产都能干净安装，审查命令链通过，说明和校验值匹配公开资产，Tag 所在 commit 的
跨平台 CI 通过。

### `v0.2.0-alpha.2/alpha.3`——有界 Codex 输入目标

- 显式选择一份 Codex 导出或宿主提供的来源集合。
- 诚实记录覆盖与排除范围，不扫描无关历史或主目录。
- 兼容现代 Codex 导出形态，同时不削弱 v0.1 的显式输入边界。
- 用第二个维护者自有 named-Skill 案例验证相关性和误匹配控制。

退出门：非专业用户能点名目标、提供或批准有界来源，并理解读了什么、排除了什么、保留了什么、
推断了什么和还有什么未知。

### `v0.2.0-beta.1/beta.2/beta.3`——安装版三模式复盘闭环目标

- 安装包支持一次性、日报、周报的初始化与校验。
- 准确工作日／周窗口、IANA 时区和有来源的生态证据。
- 稳定候选 ID、延续和重复复盘去重。
- 定时使用仍然只分析；实施仍需要当前可见授权。

退出门：安装包的三种模式都能生成可追溯报告，重复运行不会静默丢失或重复未解决候选。

### `v0.2.0-rc.1/rc.2`——绑定审查交接目标

- 私有审查包绑定目标、来源、候选状态和最终报告精确字节。
- 交给 Codex 实施前立即复验已经批准的报告。
- 报告不匹配、过期、缺来源或目标变化时失败即停止。
- 一张可见改动卡保留有效行为，并定义成功、边界、回滚和推翻案例。

退出门：交给 Codex 的报告能证明就是用户批准的报告，并能用同一案例比较修改前后，同时不赋予
隐藏授权。

### `v0.2.0-rc.3`——完整发布候选目标

- Python 3.10–3.13 的 Linux、macOS、Windows CI。
- wheel/sdist 干净安装、确定性 Demo、路径／隐私门禁和迁移说明。
- 一次性／日报／周报合成 Demo，加维护者自有真实使用证据。
- README、Skill、包元数据、Schema、限制和发布资产在同一个 commit 上一致。

退出门：同一个 commit 上每一项 v0.2 承诺都通过发布清单。任何失败都会停止稳定版发布。

### `v0.2.0`——稳定三模式产品决策

只有代码、文档、示例、安装包、公开 CI 和下载后复验一致时，候选版才能成为 `v0.2.0`。稳定版
表示可靠的产品承诺，不表示自主修改或完美诊断。

逐日顺序和门禁见[更新地图](UPDATE_MAP.zh-CN.md)。

## v0.2 之后

- `0.2.x`：每 5—10 天做一次真实维护复核；只有证据支持变更时，才发布保持兼容的 Bug、隐私、
  解析器、打包或文档修复。
- `0.3.0`：只有 v0.2 复盘闭环取得真实使用证据，且扫描边界经独立验证后，才加入可选的确定性
  Skill 健康扫描。
- `1.0.0-rc.1`：为所有准备进入 1.0 的承诺提供稳定公开契约与迁移路径。
- `1.0.0`：只有完整稳定契约在一个公开 commit 上全部通过后才发布。

人工确认的语义聚合、结构化测试适配器、其他 provider 和可选采用记录仍属于未来工作。补丁版没有
固定数量；真实 Issue、复现、安全需要和连贯功能决定是否发布，无变化时期不制造 Git 噪声。

## v0.2 冲刺明确不做

- 无人监督的代码或 Skill 修改。
- 自动 commit、push、Issue、PR、Release、上传或遥测。
- Web 账号系统、托管看板或团队权限。
- 声称模式隐私检查可以让产物天然适合分享。
- 通用沙箱、事务 apply 或自动回滚引擎。
