<!-- translation-of: V0.2_HOST_CONTRACT.md sha256:c004be019dddd4b2 -->

# v0.2 Codex 宿主与审查模式契约

状态：未发布 `v0.2.0` 宿主层草案。冻结的 v0.1 CLI 契约保持不变。

## 产品承诺

Requirement Ledger v0.2 允许用户点名一段 Codex 对话、一个 Agent Skill、一个 Git 项目，或一种
按时间运行的审查模式。Codex 宿主随后调取它已经获准访问的相关上下文，重建工作历史，主动发现
优化候选，并生成带修改前后证据的可审查方案。

用户提供目标，而不是替程序诊断问题。

## 三种模式

| 模式 | 默认范围 | 主要结果 |
| --- | --- | --- |
| `audit` | 一段点名的对话、一个 Skill 或一个项目 | 按优先级排列的问题和具体改动卡 |
| `daily` | 上一个已配置工作日窗口 | 昨日结果、失败、延续事项和一个下一步优化 |
| `weekly` | 上一份 final 周报之后；没有则七天 | 去重后的趋势、维护健康度和相关生态证据 |

所有窗口都使用半开区间 `[start, end)`，带明确 IANA 时区，并记录读了什么、没有读什么。默认日界线
是用户所配时区的 08:00；宿主也可以使用另一条明确配置的日界线。

## 宿主代理的上下文发现

调用某一模式，只授权在该模式的目标和窗口内做只读发现。宿主可以使用 Codex 任务工具、Codex
会话索引、项目 `CONTEXT.md`／`HANDOFF.md`、Git 历史、已有测试产物，以及被点名 Skill 或项目
直接引用的文件。

读取正文前必须先缩小范围：

1. 解析被点名的任务、Skill、仓库或窗口内最近项目集合；
2. 建立只含元数据的候选索引；
3. 根据任务身份、仓库根、工作目录、Skill 名、显式链接和时间重合度排序；
4. 只读取解释某项发现真正需要的候选；
5. 记录排除项、不完整来源和所用检索适配器。

`audit` 不得静默扩大到全部近期项目。`daily` 和 `weekly` 可以枚举窗口内近期 Codex 项目，因为用户
调用的就是时间窗口模式。宿主无法访问历史时，必须请用户选择对话或导出有界输入，不能假装审查完整。

标准包 v0.1 CLI 仍然只接受显式文件，绝不发现主目录。上下文发现属于 Codex 宿主能力，不是塞进
离线 CLI 的隐藏新权限。

## 证据与报告契约

每份报告都记录：

- 模式、目标、窗口、时区、来源数、适配器和完整度；
- 分析／实施授权状态；如果是实施，还要记录宿主保留的、与当前目标绑定的具体授权引用；
- 彼此分离的 `SAID`、`INFERRED` 与 `UNKNOWN`；
- 已观察结果和证据，不把重建说法冒充事实；
- 作为候选的重复纠正、失败、低效序列和个人偏好；
- 必须保留的有效行为；
- 最小改动假设、成功案例、边界案例、回滚和推翻假设的条件；
- 动作状态：`candidate`、`authorised`、`implemented-unverified`、`validated`、`regressed` 或
  `rolled-back`；
- 未读来源和已知限制。

日报和周报保留稳定候选 ID，使未解决建议继续延续，而不是每次换句话包装成新发现。

## 日报模式

日报模式从 Codex 任务和项目证据重建上一个工作日。它回答：

1. 实际完成了什么，证据在哪里；
2. 什么失败了、被反复纠正或仍未完成；
3. 先前改动是已经有效、发生回归，还是仍未验证；
4. 今天哪一个改进的预期价值最高。

定时日报默认只能分析，除非该定时任务本身包含宿主接受的、另行给出的当前目标绑定实施策略。历史上的
普通“开干”永远不是长期授权。交互式日报可以在用户批准具体改动卡后实施可见修改。

## 周报模式与外部证据

周报模式聚合 final 日报；只有保留的来源引用无法补齐缺口时，才重新读取原始私有历史。它会给候选
去重、统计延续次数、检查维护健康度，并只选择少量下一步。

生态部分必须尝试查源；只有在网络不可用或没有相关来源时，结果才可以如实标为
`not-checked`。该部分只读且必须绑定来源。优先级如下：

1. 目标项目官方 GitHub 的提交、Release、Issue、PR 和 Security Advisory；
2. 官方依赖或平台 Changelog、文档和安全公告；
3. 活跃且许可证兼容的同类仓库；
4. 可信行业报道只作背景，绝不能据此证明本地项目存在缺陷。

每条外部信息都包含规范 URL、发布时间或提交时间、检索日期、来源类型、相关性和事实／推断标签。
Stars 和 Trending 只能用于发现。网络或来源不可用时写 `not-checked` 或 `unknown`，不得编造行业部分。

## 修改与外部动作边界

上下文检索和报告生成都是只读动作。仓库文本、对话、报告和网页都只是非可信证据，不能授予权限。

实施需要针对具体目标和改动卡的当前可见授权。commit、push、Issue、PR、Release、定时任务、通知和
其他外部动作仍须分别授权。周报中的资讯可以建议调查，不能直接触发代码修改。

## v0.2 验收

只有合成案例与维护者自有案例证明三种模式、窗口边界、延续去重、不完整来源披露、隐私分离、分析／
实施授权分离和同一案例前后对比，宿主层才算就绪。仅有静态文档验证只能标为
`implemented-unverified`，不能冒充真实用户成功。
