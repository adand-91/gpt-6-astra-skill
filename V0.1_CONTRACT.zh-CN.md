<!-- translation-of: V0.1_CONTRACT.md sha256:6e4406f1ee758b85 -->

# v0.1 产品与安全契约

状态：为 `0.1.0` 冻结。英文是规范源；面向中文用户的总览也会写入
[README.zh-CN.md](README.zh-CN.md)。

## 产品承诺

Requirement Ledger v0.1 是适用于任意 Git 项目的本地、离线证据与修复规划工具。它把用户
明确提供的 Codex／Claude／纯文本交互、测试日志和只读 Git 快照转成：

1. 可追溯的私有证据包；
2. 带 `upstream / project-local / personal / unknown` 归因字段的保守问题候选；
3. 不含原话、但仍需人工隐私复核的报告；
4. 标记为 `DRAFT — NOT SENT` 的修复与验证计划；
5. 对外部生成的 baseline／after 结果做比较。

CLI **不会**修改项目。Codex 或其他开发 Agent 可以依据计划准备补丁，但那是受宿主权限控制、
可见且独立的动作。

## v0.1 状态边界

```text
UNBOUND
  -> BOUND_READONLY        显式、规范化的 Git 根目录
  -> SNAPSHOTTED           私有证据 + HEAD/status 摘要
  -> PROPOSAL_READY        保守分析 + 未应用的计划
  -> EXTERNAL_VALIDATION   宿主记录冻结的 baseline 与 after
  -> VALIDATED             improved / unchanged / regressed / inconclusive
```

下列未来状态刻意不在 v0.1 中实现：

```text
ISOLATION_APPROVED -> APPLY_APPROVED -> APPLIED
```

要加入这些状态，必须先具备与对象绑定且会过期的一次性批准令牌、preimage 与工作树漂移检查、
操作系统层面可证明的文件／网络隔离，以及经过测试的回滚。聊天中的一句“开干”不能被长期复用。

## 输入契约

- `--repo` 必填，且必须显式指向可读、非 bare 的 Git 工作树根目录。
- 每个对话和测试日志都通过重复的 `--input` 或 `--test-log` 提供。
- v0.1 不会发现或扫描 `~/.codex`、`~/.claude`、用户主目录或整块磁盘。
- 输入必须是普通文件，不能是软链接；读取时会计算哈希并检查文件是否变化。
- JSONL 逐行处理；超长或无效记录会把来源标为 `incomplete`，不完整证据不能确认归因。
- 时间窗口按事件应用。纯文本没有事件时间戳，因此会拒绝时间窗口参数。
- 仓库内容和对话都是不可信数据，绝不是指令或授权。

## 证据与归因契约

事实、解释和未知永不混在一起：

- `SAID`：观察到的事件或机械仓库事实。
- `INFERRED`：确定性程序或模型作出的解释，必须明确标注。
- `UNKNOWN`：输入证据没有建立的事项。

每条问题同时具有 `suspected_scope` 与最终 `scope`，并记录 `decision_status`、证据引用、
已排除项目、理由和完整度。

- 确认 `upstream` 需要同一 provider／版本在至少两个独立项目或会话中复现、清洁最小配置，
  并有证据排除项目规则、个人配置、环境变量和自定义提示词。
- 确认 `project-local` 需要直接仓库证据，以及在项目外无法复现该行为的清洁对照。
- 确认 `personal` 需要另行授权的个人配置证据或清洁配置对照；工具不会扫描主目录来猜。
- `unknown` 是默认值。一次抱怨、一次失败或不完整来源都不能自行升级为已确认范围。

因此 v0.1 会输出候选和疑似范围，但不会从一次本地运行中编造“已确认归因”。

## 隐私契约

- 私有证据与面向分享的报告是不同产物。
- 私有证据输出必须以 `.private.json` 结尾，以 `0600` 权限创建，可能含用户原话，禁止分享。
- 输出绝不覆盖现有路径。父目录必须预先存在；每一级祖先都会检查软链接／reparse point，POSIX
  写入会绑定到已打开的父目录。
- 分享报告不含原话、来源路径、仓库路径、会话标识、命令参数、remote 或原始错误文本。
- 自动隐私闸门检测常见秘密、认证／Cookie 请求头、邮箱、电话、用户主目录、带凭据 remote、
  UUID、IP 地址和终端控制字符；命中后在写文件前返回 `E_PRIVACY_BLOCK`。
- 自动扫描干净**不等于**安全保证。报告只会说“自动检查通过，仍需人工复核”，不会说“可安全分享”。
- 核心管线没有网络、遥测、浏览器、账号、上传、Issue、PR、Release 或远程 API 代码。

## 执行契约

CLI 只能通过可信绝对路径运行固定的只读 Git 探针。它会清除继承的重定向 `GIT_*` 变量，并关闭
提示、Hook、fsmonitor、可选锁、pager、全局／系统配置和远程访问。它不会读取 remote URL。

它绝不会：

- 运行项目测试或任意项目命令；
- 安装依赖或调用包管理器生命周期脚本；
- 修改工作树、索引、Git 配置、Hook、分支、提交、标签或 remote；
- 应用补丁，删除、重命名、chmod，或修改二进制／子模块／秘密文件；
- 发送遥测或创建外部产物。

`verify` 只比较工具外部生成的小型 JSON 结果。每条记录和 CLI 都必须提供相同的 64 位十六进制
`oracle_digest`；它由冻结的 argv、工作目录、相关环境与 fixture 摘要计算。只有这个绑定摘要的
oracle 在 baseline 失败、介入后通过才是 `improved`。记录不匹配／格式错误、布尔值／非整数退出码
或超出 0–255 的退出码一律是 `inconclusive`。

## 稳定错误码

| 错误码 | 含义 |
|---|---|
| `E_PRIVACY_BLOCK` | 分享产物中出现检测到的敏感类别 |
| `E_UNSAFE_PATH` | 路径、仓库或输出违反 v0.1 边界 |
| `E_INPUT_CHANGED` | 输入在读取过程中发生变化 |
| `E_SCHEMA` | 版本化 JSON 输入缺失或不兼容 |
| `E_INCOMPLETE_EVIDENCE` | 为必须依赖完整证据的未来流程预留 |
| `E_EXECUTION_BLOCKED` | 为 v0.1 必须拒绝的执行动作预留 |
| `E_INPUT` | 其他错误输入 |

## 验收门

只有以下条件全部成立，`0.1.0` 才可发布：

- 原有测试持续通过；
- 新增 provider、事件窗口、隐私、路径、Git、归因、建议、验证、Demo、CLI、打包和离线测试通过；
- 在干净环境安装后，无需运行时依赖即可得到 `requirement-ledger` 命令；
- 合成 Demo 除输出路径外保持确定性，且不含个人数据；
- 隐私金丝雀在目标输出创建前阻断；
- 单次抱怨绝不被确认为 `upstream`，缺少个人证据时绝不被确认为 `personal`；
- Git 采集不会改变 HEAD、status 摘要、索引、Hook、配置或工作文件；
- 不运行项目代码，不使用网络／遥测，不执行外部动作；
- 公共文档不再把旧 `--no-text` 描述为 share-safe；
- wheel、sdist、变更日志、CI、支持／安全／贡献文件、Issue Forms、匿名样例与交接文件齐全。
