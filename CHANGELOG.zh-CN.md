<!-- translation-of: CHANGELOG.md sha256:7d86659f6ec27dfb -->

# 变更日志

本项目的显著变化均记录在这里。版本遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

## [1.0.2] - 2026-09-09

### 变更

- 将公开产品显示名确认为 **Astra Skill Optimizer**，并保留内部 optimizer 插件标识作为兼容入口。
- 将本次改名整理为新版本，保持历史 `1.0.1` 记录不变。


## [1.0.1] - 2026-09-09

### 变更

- 将面向 GPT-6／Astra Skill 适配的公开产品显示名改为 **Astra Skill Optimizer**。
- 保留历史插件 ID、包模块和 CLI 作为兼容入口，同时新增 `gpt6-astra-skill` 命令。
- 统一仓库文档、marketplace 显示名和发布说明中的新产品身份。


当前本地版本源为 `1.0.0`。这是通过测试的本地稳定候选，不是 Tag 或 GitHub Release。

### 修复

- 第一次真实 macOS dogfood 暴露 Homebrew Python 的 PEP 668 安装边界和首用路径过于技术化后，重新开启
  小范围本地资格验证。README 现在在 macOS/Homebrew 推荐已实测的隔离命令 `uv tool install .`，把普通
  `pip install .` 限定在已激活的虚拟环境中，且绝不建议 `--break-system-packages`。
- 把插件新手入口如实拆成两级。Codex 宿主已选快速审查不需要额外时间窗、JSONL、范围根或文件路径，
  但必须标为 `host-selected / unbound`，且不会生成 CLI 绑定。证据绑定 CLI 路径继续要求明确来源，
  v1 `review-init --mode audit` 仍要求起点、终点和时区。
- 真实 dogfood 证明，本地 marketplace 源码改动后，Codex 实际加载的版本化 Skill 快照仍可能是旧字节；
  因此新增全新任务回归，并要求本地迭代先显式 remove/add 刷新，再新开任务确认。
- README 改为先讲产品结果、60 秒 Codex 审查、两级工作流、插件精确刷新和基于事实的 Codex-first 定位。
  Anthropic 公开 Skills 只作为打包调研，不复制竞争项目文本或代码。

### 兼容性

- 未改变版本、Python 运行时、schema、CLI 命令或授权边界。插件 manifest 的说明和默认提示只用于准确
  描述现有两级入口。这个本地资格更新没有 commit、push、打 Tag、创建 Release 或公开提交。

## [1.0.0] - 2026-08-30（本地稳定候选）

### 变更

- 不增加功能，也不改变 schema、命令、授权边界或运行行为，把经过独立安全复核的 RC 表面晋级为
  稳定版。
- 完成稳定 Python 包元数据和对应 `1.0.0` 纯 Skill Codex 插件。
- 用稳定的核心／插件契约、本地安装链路、平台证据边界以及与公开发布明确分离的说明替换预发布指引。

### 验收边界

- 完整源码与解包 sdist 测试、干净 wheel／sdist 安装、audit／daily／weekly 链路、精确绑定／
  交接正负用例、归档检查、插件校验，以及真实本地 Codex marketplace 添加／安装／移除周期共同
  构成本地稳定门。
- Windows 原生 API 与故障注入用例仍是必须执行的 Windows CI 门；macOS 跳过结果会明确记录，
  不会冒充 Windows 运行证据。
- 本地候选不包含 commit、push、Tag、Release、公开 CI、插件提交、推广或外发消息。

## [1.0.0rc1] - 2026-08-30（本地 RC 候选）

### 新增

- `review-bind`：生成私有 `review-binding/v1` 身份，绑定最终报告准确字节、目标、当前已验
  来源包、候选状态和进入完整性头的时间戳。
- 只读 `review-handoff-check`：重新打开每个明确输入，只有报告、来源、候选、目标和绑定身份
  仍完全一致时才输出稳定的 ready 标记。
- Windows HANDLE 相对输出后端：绑定父 `HANDLE`，使用 `NtCreateFile(RootDirectory=...)`、
  受保护 DACL、delete-pending 提交、同身份域父目录复验和纯 HANDLE 回滚。

### 安全与兼容

- 不完整证据不能进入普通交接；显式归档选项只保留不完整身份，绝不授予实施或外部动作权限。
- 报告必须包含人眼可见、非占位的 Markdown 证据。隐藏 HTML 注释、顶层或列表／引用容器内围栏、
  未知 frontmatter 控制字段、目标不一致、过期头、字节漂移、同文件原地变化、持久化状态超限和
  不安全输出原语均失败即停。
- v0.1、Alpha 3、Beta 1、Beta 2 命令和 schema 继续受测试覆盖。Windows 原生 API 测试已经
  提供，但本 macOS 候选会如实跳过，等待以后在 Windows CI runner 执行。

## [0.2.0a3] - 2026-08-30（本地预发布候选）

### 新增

- 新增可选的 `codex-scan` 命令：把唯一显式 Codex JSONL 导出绑定到用户选择的非主目录范围根、
  目标／任务引用、IANA 时区和半开窗口。
- 私有证据包内嵌 `codex-input-envelope/v2`。它记录准确来源摘要和字节数、哈希后的目标／任务绑定、
  物理记录核算、固定纳入／排除代码，以及诚实的部分／未知历史覆盖；不保留来源原文、文件名或路径。
- 严格范围输入打开器：逐组件拒绝普通符号链接／reparse，拒绝硬链接；POSIX 使用
  `dir_fd`／`O_NOFOLLOW` 逐级打开，并重复核对身份。仅在 macOS，Apple 固定的根兼容别名会
  规范到 root 持有的 `/private` 目标。
- 基于官方来源的 Codex 对齐调研，覆盖 Codex for OSS、Skills、`AGENTS.md`、审批、插件和当前
  兼容性影响。
- 新增 `codex-modern-normalization/v1`：支持官方 `item_completed` TurnItem 白名单与
  `task_complete` / `turn_complete` 终点，并加入真实点名 Skill 形状的隐私 fixture。

### 变更

- Codex 专用路径最多只捕获一次 64 MiB 且 1,000,000 条物理记录的输入，并从同一份
  字节计算摘要和解析；它不枚举范围根，也不发现其他历史。
- Codex 候选窗口现在按文档使用半开 `[start,end)` 边界，刚好等于 `until` 的事件会被排除。
- 重复完成项目快照保留第一次物理位置，由最后一份合法结构化快照提供状态；同一 item ID 位于不同
  turn 时仍保持独立，普通消息和工具事件不做内容去重。
- automation、delegation、Subagent、system 和非证据元数据记录只按准确结构分别计数，不从
  自然语言推断。
- 快照 ID 使用规范元组哈希，保留／丢弃的 item 与 terminal 记录分开计数；信封校验器要求
  这些子计数与已识别记录精确守恒。

### 安全

- 文件系统根、用户主目录本身、目录、父级穿越、范围越界、符号链接／reparse 组件、硬链接、输入
  边界漂移和超限 Codex 输入，都会在创建输出前停止。
- 信封不会把一份选中导出升级成“完整 Codex 任务历史”；上游协议非穷尽且本候选使用固定白名单，
  因此语义归一化仍明确标为 `partial-alpha.3`。
- 缺失／非法时间戳、损坏记录、不支持记录和超限记录都会让选中来源与完整证据标为不完整，所以下游
  分析不能从只完成部分归一化的证据确认候选。
- 带控制字符的 rollout ID、未知／畸形 `UserInput` 块和非法状态／类型组合会被标为不支持，
  不会被静默合并、丢弃或提升为完成状态。
- 摘要与解析绑定到同一份已捕获字节缓冲。并发来源路径稳定性明确只是基于元数据的尽力检查，不声称
  原子快照；工具不启动网络客户端，但信封不判断远程文件系统。
- 现有 v0.1 `scan` 行为继续可用；更强的 Codex 契约是显式新命令，不会静默改变冻结的 v0.1 CLI
  路径。

## [0.2.0b1] - 2026-08-30（本地预发布候选）

### 新增

- 安装后的 `review-init` 现在支持 `audit`、`daily`、`weekly` 三种实用模式。
- 可根据明确 IANA 时区、带偏移的参考时刻和本地边界小时，计算最近完成的每日／每周半开窗口；
  同时保留显式起止窗口。
- 夏令时处理具有确定性：不存在的本地边界时间前移到第一个有效分钟，重复边界时间选择第一次出现。

### 安全与兼容

- 所有新骨架仍为零来源、私有、禁止覆盖、仅分析，并通过同一个 `review-check` 契约；没有增加
  历史发现或联网调用。
- 旧 audit API 和 audit CLI 输出标记保持兼容。
- 非法模式、不成对显式窗口、无偏移时刻、冲突的参考时刻／窗口、非法时区和非法边界小时会在
  不输出文件的情况下失败，且不打印 traceback。

## [0.2.0b2] - 2026-08-30（本地预发布候选）

### 新增

- 严格私有 `candidate-ledger/v1` 快照：稳定不透明 ID、冻结状态转换、目标哈希、确定性状态头、
  过期头拒绝和未解决候选项精确延续。
- `source-pack` 与只读 `source-verify`：只对同一个非主目录范围根之下被明确选择的文件记录
  无路径 SHA-256 和字节数绑定。
- `candidate-sync`：从有界 `candidate-current/v1` 输入生成禁止覆盖的私有状态。
- 仓库内 Codex marketplace 与轻量纯 Skill 插件，用于引导已经安装的 CLI。

### 安全与兼容

- 候选项只按 ID 精确匹配，不做语义合并，也不会静默丢弃仍处于 `candidate`、`authorised`、
  `implemented-unverified` 或 `regressed` 的项目。
- 范围输入拒绝根／主目录、父级穿越、越界、链接、硬链接、重复物理文件、读取中替换、超数量和超字节。
- 来源包不保留目标原文、路径、文件名、来源文本、设备或 inode；摘要只证明字节身份，不证明事实、
  作者、完整性或授权。
- 插件不含 MCP、App、Hook、复制的 Python 核心、安装器或自动更新器；CLI 缺失／不兼容时明确停止。

## [0.2.0a1] - 2026-08-30（预发布版）

### 新增

- 面向一次性审查、每日优化和每周维护／生态复查的 Codex-first 宿主契约。本 Alpha 的安装版
  CLI 只开放一次性审查骨架初始化；日报和周报仍是已文档化的契约与模板。
- 有界 Codex 上下文发现规则：用户点名目标或时间窗口，宿主自动找到相关且已获授权的历史，
  不要求用户重新讲问题。
- `audit`、`daily`、`weekly` 三种模式的双语指南和报告模板。
- 零依赖审查报告契约校验器：校验真实 ISO-8601 窗口先后、IANA 时区、目标绑定授权引用和结构化周报
  来源字段，并包含中英文模板回归测试。
- 新手优先的 Agent Skill 个性化工作流，包含人话改动卡、保留能力清单、成功场景、边界场景和回滚。
- 从本 Alpha 到稳定 `v0.2.0` 决策的双语、逐门禁十天更新地图。

### 变更

- README 与 Skill 现在优先表达理解上下文的个人优化：点名对话、Skill、项目或审查窗口，再由
  Codex 恢复相关历史。
- `v0.2.0` 路线把三种用法统一在一个项目里，不拆成三个重复仓库。
- 定时日报和周报默认只分析；实施和外部动作仍需要当前目标绑定授权。

### 修复

- 点名目标不再必须先塞进 v0.1 项目证据报告结构才能开始审查：`review-init` 会生成专用审查骨架。
- 审查文档不再只靠“看起来完整”：时间窗口、时区、必需章节、证据标签、候选状态或授权声明
  不合格时，`review-check` 会失败。
- 独立检查脚本不再维护第二份校验规则，而是调用包内唯一实现。
- 新审查骨架不会覆盖已有输出；平台支持时使用私有文件权限。
- Windows 现在按平台条件安装标准 `tzdata` 数据库，使同一个显式 IANA 时区在 Linux、macOS、
  Windows 上都能校验。Windows 未安装依赖时会返回可执行错误，不再把“缺时区数据库”误报成
  “时区无效”。

### 兼容性

- 已发布的 `v0.1.1`、标准包 CLI、v1 Schema、包名和冻结 v0.1 安全契约保持兼容。本预发布版
  新增可选的 `review-init` 与 `review-check` 命令；它不会发现 Codex 历史，也不会赋予实施授权。
  类 Unix 系统继续保持零运行时依赖；Windows 使用 Python 标准库 `zoneinfo` 所需的条件依赖
  `tzdata`。

## [0.1.1] - 2026-08-28

### 新增

- 面向 AI 项目与 Skill 改进工作流的 Requirement Ledger AI 定位。
- 从证据原型到 `v1.0.0` 技术正式版的双语 15 天路线图。
- 一个完全合成、可复现的 Skill 改进案例，使用由外部运行、摘要绑定的前后 oracle。

### 变更

- 优化 README 的发现元数据、徽章、搜索词、首次使用表达和 Skill 案例入口。
- 包元数据现在让 CLI 与记录版本共同读取唯一的 `__version__` 来源。
- 源码分发包明确包含双语路线图。

### 兼容性

- 仓库 slug、包名、命令行入口、CLI、Schema 与 v0.1 安全边界均未改变。

## [0.1.0] - 2026-08-28

### 新增

- 可安装、零运行时依赖的标准包与 `requirement-ledger` 命令。
- 显式输入的 Claude、Codex 和纯文本事件规范化。
- 版本化证据、问题、建议与验证记录。
- 不读取 remote 的只读 Git 快照。
- 私有证据、保守四类归因、无原话报告与修复草案。
- 合成端到端 Demo、CI 矩阵、Issue Forms、安全／支持／贡献政策、维护策略、威胁模型、架构、
  范围账本与发布检查清单。

### 安全

- 新工作流不发现主目录，也不运行项目代码。
- 分享报告使用失败即停止的自动隐私闸门，并且仍需人工复核。
- 修复建议始终为 `DRAFT — NOT SENT` 与 `not-applied`。
- 生成的反馈在真人发送之前始终为 `DRAFT — NOT SENT`。
- 本地动作始终是建议，绝不自动应用。
- CLI 不运行项目代码、不安装依赖、不联网、不遥测，也不执行 commit、push、Issue、PR、
  Release 或上传。
- 仓库文本固定为 LF，确保 Windows 上双语哈希确定。transcript 与测试日志绑定均将同一文件身份
  与跨平台稳定的内容元数据分开验证。
