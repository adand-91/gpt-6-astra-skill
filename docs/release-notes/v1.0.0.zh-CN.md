# v1.0.0 本地稳定候选——有界证据、准确交接与 Codex 分发

日期：2026-08-30。状态：本地稳定源码与分发候选；没有 commit、Tag、push、公开发布或提交公开
插件目录。

## 稳定产品

Requirement Ledger v1.0.0 由独立 Python CLI／库核心和轻量纯 Skill Codex 插件组成。核心唯一负责
有界输入选择、私有来源包、明确候选延续、可见报告校验、报告／来源／状态精确绑定和只读交接复验。
插件只引导已经安装且兼容的核心；不含 MCP、App、Hook、安装器、更新器、模型运行时、凭据、遥测或
联网客户端。

## 稳定链路

受支持链路为 `review-init`／`review-check` → `source-pack`／`source-verify` →
`candidate-sync` → 最终报告 → `review-bind` → `review-handoff-check`。audit、daily、weekly
只接受宿主／用户选定的目标、时间窗、范围根和明确文件。

ready 只表示当前字节／状态身份一致，不能证明事实、作者、完整性、质量、批准，也不授权编辑、
commit、发布、发送、计划任务或提交任何内容。

## 晋级事实

重建 RC 通过独立安全复核后，没有新增功能或 schema。稳定晋级只改变版本／分发元数据和文档，
随后重新运行完整本地资格验收。由于本地候选产自 macOS，Windows 原生 API 测试仍必须以后在
Windows CI 中执行。

公开 GitHub Release 与公开 Codex 插件提交仍是维护者以后单独决定的动作。

## 本地验收

最终源码套件收集 194 项：187 通过，七项 Windows 原生测试在 macOS 跳过。解包 sdist 同样收集
194 项，并明确跳过十项：相同的七项 Windows 原生检查，以及三项只适用于仓库分发层的插件检查。
干净 wheel／sdist 安装均报告 `requirement-ledger 1.0.0`；audit、daily、weekly、漂移、不覆盖、
交接、归档、插件校验和真实本地 Codex marketplace 生命周期检查全部通过。精确归档哈希与命令证据
保存在源码树外。
