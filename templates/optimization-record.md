# Optimisation record — 2026-08-17

<!-- One file per round. The outcome field is filled in LATER, by the next round. -->
<!-- A record whose outcome is still empty after several rounds is itself the finding. -->

**Evidence** — what triggered this, with counts and quotes.

- 「又得我提醒你」×3 on the same topic, 08-12 / 08-14 / 08-16 `SAID`
- 13 hand-runs of the same command across 4 sessions (facts)

**Layer** — new Skill / edit an existing Skill / script / project rule / global rule / nothing.

- Edit an existing Skill. A new one would overlap `<name>`.

**Diagnosis** — `NO RULE` / `RULE DID NOT FIRE` / `RULES CONFLICTED` / `WRONG PLACE` /
`TOOL LIMIT` / `ONE-OFF`, and why that one.

- `RULE DID NOT FIRE`. The rule exists but its trigger uses vocabulary the user never types.

**Change** — what was actually edited, by path. Nothing here until authorised.

- `path/to/SKILL.md` — trigger reworded to the user's own words; boundary added.

**Expected** — what should now be different, stated so it can be checked.

- The reminder stops recurring. If it recurs once more, the diagnosis was wrong.

**How it will be checked** — the command, the window, or the observation.

- Next retrospective: `--since` this date, expect 0 occurrences of that quote.

**Risk / cost** — what this adds to every load, and what could get worse.

- +18 lines on every load of that Skill. Risk: the wider trigger may fire on unrelated tasks.

**Outcome** — left blank until the next round measures it.

- <!-- worked / no change / made it worse, with the number that shows it -->
