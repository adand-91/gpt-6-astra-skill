<!-- translation-of: templates/optimization-record.md sha256:60401ffa0a2d5b20 -->

# 优化记录 — 2026-08-17

<!-- 每轮一个文件。「结果」栏是**后来**由下一轮填的。 -->
<!-- 跑了好几轮结果栏还空着，这本身就是发现。 -->

**证据** —— 什么触发了这一轮，带计数和原话。

- 同一话题上「又得我提醒你」×3，08-12 / 08-14 / 08-16 `原话`
- 同一条命令跨 4 个会话手跑 13 次 (facts)

**层级** —— 新 Skill / 改已有 Skill / 脚本 / 项目规则 / 全局规则 / 不做。

- 改已有 Skill。新建会和 `<名字>` 重叠。

**诊断** —— `规则缺失` / `规则没触发` / `规则冲突` / `位置错误` / `工具限制` / `一次性`，
以及为什么是这个。

- `规则没触发`。规则存在，但触发词用的是用户从来不会打的词汇。

**改动** —— 实际改了什么，写路径。没授权之前这里什么都不写。

- `path/to/SKILL.md` —— 触发词改成用户自己的原词；补了边界。

**预期** —— 现在应该有什么不同，写成能被检查的样子。

- 那句提醒不再出现。如果再出现一次，说明诊断judgement错了。

**怎么检查** —— 命令、窗口，或者观察方式。

- 下次复盘：`--since` 本日期，预期那句原话出现 0 次。

**风险 / 代价** —— 它给每次加载增加了什么，什么可能变糟。

- 该 Skill 每次加载 +18 行。风险：放宽后的触发可能在无关任务上误触发。

**结果** —— 留空，等下一轮量出来再填。

- <!-- 起作用 / 没变化 / 更糟了，附能说明这一点的数字 -->
