<!-- translation-of: templates/retro-report.md sha256:8a96d4a75e75f4a2 -->
---
window: 2026-08-10T08:00:00+08:00 .. 2026-08-17T08:00:00+08:00
scope: myproject
engines: claude, codex
facts: /tmp/facts.json
---

# 复盘 — myproject

<!-- 每条结论带 原话 / 推断 / 未知。每个数字带 (facts) 或一个路径。 -->
<!-- 校验：python3 scripts/check_retro_report.py 本文件.md --lang zh -->

## 实测

<!-- 直接来自脚本。不要重打、不要四舍五入、不要添。 -->

- 会话 23 个 用户回合 116 助手回合 1705 (facts)
- 工具调用 785 次 失败 20 次 (facts)
- 纠偏被标记 17 条 判定为真 9 条 (facts)
- 字节 22,656,379 分布在 23 个会话 (facts)

## 真需求

**长期目标** —— 一句话说清这整件事是为了什么。基于下面那些纠偏 `推断`。

- 「不是让你重写 我就想让它别每次都问我一遍」 —— 2026-08-14T21:12+08:00 `原话`
- 验收动作从没被说出来过。`未知` —— 只有用户能回答。

**说过三遍以上**（价值最高的条目 —— 被重复的事就是从没修好的事）：

- 「又得我提醒你」×3，08-12 / 08-14 / 08-16 `原话`

## 需求去向

<!-- 从第一条用户消息往后走，不从最近的工作往回写。 -->

| # | 需求 | 去向 | 依据 |
|---|---|---|---|
| 1 | … | `已完成` | 产物路径 |
| 2 | … | `部分完成` | 具体缺什么 |
| 3 | … | `静默丢弃` | 08-11 之后再没被提过 |

## 错误

| 代价 | 发生了什么 | 层级 | 修法 |
|---|---|---|---|
| 20 轮 (facts) | 假设了错误的输出格式 全程没标出来 | `规则缺失` | … |
| 4 轮 (facts) | 规则在一个读得太晚的文件里 | `位置错误` | 搬位置 |
| 0 | 网络抖动 | `一次性` | 不修 —— 不要为它写规则 |

**用户替我们付的账**：用户不得不重复 3 次，并抓到一条被报成已完成而其实没有的。`原话`

## 可自动化候选

| 次数 | 模式 | 结论 | 为什么 |
|---|---|---|---|
| 13次 (facts) | 每次改完手跑 `python3 assemble_final_v2.py` | 脚本 不是 Skill | 不涉及判断 |
| 3次 (facts) | 同一话题上的「又得我提醒你」 | 改已有 Skill | 触发词汇就在那几句原话里 |
| 48次 (facts) | `Bash → Bash → Bash` | 不做 | 这是活的形状 不是工作流 |

## 未完成 / 未知

- … `未知` —— 谁能回答。

## 下一个动作

一个动作。不是一张清单。
