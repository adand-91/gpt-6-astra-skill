<!-- translation-of: MAINTENANCE.md sha256:9d9b9e8bf4573e51 -->

# 维护策略

Requirement Ledger 依靠证据维护，不做活跃度表演。每个版本必须对应一组完整、经过测试的改动。
项目不回填假日期、不制造空提交，也不把一项已完成改动拆成没有意义的多个版本来显得繁忙。

## 分诊节奏

- 每周：审查新 Issue、Discussion、失败和用户纠正；标记为 `upstream-candidate`、
  `project-local`、`personal`、`unknown`、文档、安全或功能请求。标签不等于确认。
- 每月：发布一个真实维护版本，或发布简短状态说明，解释调查过什么、为什么无需发版。
- 每季度：复查路线图、支持的 Python／provider 版本、同类项目、贡献者体验、隐私边界和采用证据。
- 安全：及时私下分诊可信报告；经过验证的修复与回归测试准备好后尽快发布补丁。

每日私有总结和每周生态复查可以帮助维护者，但不会自动 commit。没有变化的日子不应制造 Git 噪声。

## 版本策略

项目在 1.0 之前遵循语义化版本：

- `0.1.x`：兼容的 BUG、隐私、解析器、打包与文档修复；
- `0.2.0`：结构化测试适配器、问题聚合与日报／周报输出；
- `0.3.0`：只有隔离后端、对象绑定批准、冻结 oracle 执行、漂移检查和回滚故障注入得到证明后
  才进入；
- `1.0.0`：Schema 与 CLI 稳定，有迁移策略、多个真实项目案例和持续维护记录。

安全修复可以加快节奏；功能压力永远不能削弱安全门。

## 分批发布计划

### A 批——`v0.1.0`：可安装证据闭环

标准包与 CLI、显式输入对话适配器、只读 Git 快照、隐私分层、保守归因、修复草案、外部验证
比较、合成 Demo、CI、社区文件、双语核心文档，以及原有复盘兼容入口。

### B 批——`v0.1.x`：真实使用加固

只处理首批真实本地使用中有证据的问题：provider 格式漂移、隐私误报／漏报、Windows／macOS／
Linux 打包结果、错误提示与回归测试。不会预先规定要发多少个补丁版本。

### C 批——`v0.2.0`：报告与适配器

结构化 JUnit／pytest／TAP／cargo／Go 适配器、人工确认的问题聚合、每日改进报告、每周项目／
生态复查，以及默认无遥测的可选采用记录。

### D 批——`v0.3.0`：受控修改

只有项目缺口账本中的安全工作得到证明后才开始，不提前承诺发布日期。

## 发布门

每个公开版本都必须满足：

1. 关联真实 Issue、安全需要或完整里程碑；
2. 有合成回归覆盖，原有与新增测试全部通过；
3. 干净安装、wheel／sdist、命令行、编译与确定性 Demo 检查通过；
4. 隐私金丝雀与原话／路径／会话／remote 泄漏检查通过；
5. 规范文档的中英文镜像校验通过；
6. 更新变更日志、已知限制、必要迁移说明与 Release Notes；
7. 最终 diff 与工作树复核，确认没有私有证据或构建产物；
8. tag、push 和 GitHub Release 分别取得发布授权。

CI 通过是必要条件，不是充分条件；最终由维护者决定发版。

## 健康维护的证据

Star、Fork、Watch 有助于发现项目，但不能证明维护质量。更强的证据包括：

- 对真实 Issue 的响应速度和解决质量；
- 版本与可复现问题、修改前后测试相对应；
- 重复用户和外部贡献者；
- 被接受的上游反馈，以及有记录的“不修”；
- 透明的安全与兼容性决策；
- 路线图持续保留未完成项。
