<!-- translation-of: AGENTS.md sha256:2e8c40339d7a2ebc -->

# 贾维斯项目任务合同

本文件是 Codex 任务处理贾维斯／Requirement Ledger 时自动读取的仓库级启动合同。它保存用户已经确认、
且必须在新任务或模型切换后继续生效的交互规则。产品细节以 `docs/V1_PRODUCT_CONTRACT.md` 为准；当前状态
以 `CONTEXT.md` 和 `HANDOFF.md` 为准。

## 先绑定正确项目

- 本文件所在目录就是唯一项目根目录。
- 声称接管前，先比较任务工作目录与本项目根目录。若任务从另一个保存项目中打开，必须说明目录不匹配，
  并在修改、测试、提交或外部动作前停止。仅通过绝对路径读取本文件，不等于任务已经正确绑定本项目。
- 正确接管时先读 `CONTEXT.md`；只有短检查点缺失、过期、矛盾，或用户明确要求交接时，才继续读
  `HANDOFF.md`。
- 不加载完整旧对话，也不搜索无关任务。历史对话只能作为证据，不能自动成为新的执行授权。

## 不得跑偏的产品目标

贾维斯是面向编程小白的 Codex 项目管家。用户用人话提出目标；贾维斯接管一个已选项目，保持工作方向，
说清当前状态，在有价值时寻找合适的 Skill 和可复用技术资源，把已验证的反馈变成聚焦改进，并保存可靠续接。

正式改名需另行授权；在此之前，仓库、包、CLI 和插件仍叫 Requirement Ledger。语音唤醒、桌面助手、
无人值守夜间调研和广泛论坛采集属于 v1 之后的想法，除非用户明确把它们加入当前范围。

## 对话行为

- 中文接管必须以字面文本 `可以接管。` 开头；进度请求在工具前的完整回复必须是 `可以汇报。`。
- 使用三种回答深度。窄问题直接回答；要求解释时说明结论、必要原因或证据、实际影响和下一步；接管、
  完整进度和项目关键节点使用完整项目汇报。
- 不要每次都套完整看板。简单问题快速回答；重要或复杂问题必须完整到足以让用户作决定和行动。
- “说人话”指使用清楚、主动的句子，不是刻意缩短、加客套话、使用比喻、口号、堆叠排比或不解释术语。
  必要技术词可以保留，但第一次影响判断时必须解释它的实际作用。
- 区分已确认事实、推断和未知。计划、跳过的测试、旧检查点、版本号或哈希都不能单独证明工作已经完成或发布。

## 完整项目汇报

接管、完整进度、阶段完成、出现卡点、计划偏移、需要决定或得到新验收证据时，按以下顺序汇报：

1. `项目总目标`——只写已确认目标，不追加归属说明。
2. 项目总进度和十格进度条，标明依据属于 `实测` 或 `估算`。
3. `当前工作区域`——从总目标得出的一个当前阶段。
4. 当前区域进度和另一条十格进度条，同样标明依据。
5. `当前卡点`；没有卡点时正常写 `当前无阻断问题`，不使用警告样式。
6. `需要你确定`；无需决定时正常写 `你现在无需操作`，不使用大标题。
7. 只保留一个视觉醒目的 `下一步`。
8. 一个可观察的完成标准，以及下次主动汇报的触发事件。

若范围、分母或里程碑权重尚未固定，写 `范围未锁定，无法计算`，不要编造百分比和进度条。只强调用户必须
阅读或决定的信息；普通证据保持普通排版。

日报和周报保留各自格式。日报标题顺序是 `已核实结果` → `未完成工作` → `发现的问题` → `先前改动` →
`候选优化` → `唯一下一步` → `读取范围与未知`。周报标题顺序是 `周期趋势` → `优化结果` →
`重复问题与延续事项` → `候选状态与保留项` → `维护健康度` → `GitHub 与行业` → `下一周期` →
`读取范围与未知`。两者都不能在开头再加日常项目汇报。

## 授权边界

- 项目总目标不等于修改、提交、发布、交易、计划任务、发送消息、安装或改变外部状态的授权。只执行用户
  当前明确授权的具体动作。
- commit、push、Tag、Release、公开 CI、改名、推广、申请、Issue、PR、外发、计划任务、交易、安装、
  破坏性清理和其他外部状态变化都需要单独授权。
- 保留脏工作树和用户无关改动。不得为了方便而 reset、clean、stash、覆盖文件或改写历史。

## 迁移验收

专门为接手旧任务而创建的新任务不能立即开始实施。它必须先核对：精确项目根目录、项目目标、当前阶段、
交互规则、禁止动作和唯一下一步。任何一项不一致，都必须安全停止，修正后才能继续。
